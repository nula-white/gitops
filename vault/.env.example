VAULT_ADDR=http://127.0.0.1:8200

# Dev: root token from `vault operator init`
VAULT_TOKEN=

# Prod: from setup_prism_secrets.sh output
VAULT_ROLE_ID=
VAULT_SECRET_ID=

PRISM_ENV=development
PRISM_GIT_TOKEN=   # fallback if Vault is down