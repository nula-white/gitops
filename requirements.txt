
# External tools (NOT pip-installable — see setup guide):
#   Joern  : ~/tools/joern/joern-cli/joern-parse
#   CodeQL : ~/tools/codeql-home/codeql/codeql
#   Java 17+: required by Joern (sudo apt install openjdk-17-jdk)

# ── Web framework ────────────────────────────────────────────
fastapi
uvicorn[standard]
websockets
python-multipart # form/file upload support for FastAPI
jinja2 

# ── Data validation ──────────────────────────────────────────
pydantic
pydantic-settings
python-dotenv

# ── Graph database ───────────────────────────────────────────
neo4j

# ── Parsing & AST ────────────────────────────────────────────
# Tree-sitter core framework
tree-sitter
# Language grammar bundle — provides all language bindings:
# Python, Java, JavaScript, TypeScript, TSX, Go, Rust, C, C++,
# Bash, Ruby, PHP, C#, and more in a single package.
tree-sitter-languages

# ── YAML (required for sink pattern loader) ──────────────────
pyyaml==6.0.1

# ── HTTP client ──────────────────────────────────────────────
httpx
aiohttp  # async HTTP for agent tool calls
aiofiles # async file I/O in sandbox layer

# ── Secrets management ───────────────────────────────────────
hvac # HashiCorp Vault client

# ── Agent orchestration (LangGraph pipeline) ─────────────────
langchain
langchain-core
langgraph

# ── Blockchain audit layer ───────────────────────────────────
web3 # Ethereum Sepolia hash logging

# ── ML / GraphCodeBERT (install separately, GPU optional) ────
# Uncomment when wiring in the GraphCodeBERT inference layer.
# torch
# transformers
# peft # LoRA adapter support
# datasets   # Devign / Big-Vul / VDISC loaders
# scikit-learn # evaluation metrics

# ── Testing ──────────────────────────────────────────────────
pytest
pytest-asyncio
pytest-cov